import aerosandbox as asb

def CoordToKulfan(datfile : str) -> list:
    """
    DRLFoil works with Kulfan parameters, so this function is used to convert the common coordinates used in airfoil design to Kulfan parameters.

    Args:
    datfile (str): The path to the .dat file containing the coordinates of the airfoil.
    """
    
    print("This is a test")
